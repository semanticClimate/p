<div class="projects-publications">

  <div class="pp-section">
    <h3 class="pp-section__title">Projects</h3>
    <div class="pp-scroll">
      {% for project in projects %}
        <a href="{{ project.url | url }}" class="pp-card">
          <div class="pp-card__media">
            {% if project.image %}
              <img src="{{ project.image | url }}" alt="{{ project.title }}" loading="lazy">
            {% endif %}
          </div>
          <p class="pp-card__title">{{ project.title }}</p>
          <p class="pp-card__desc">{{ project.description }}</p>
        </a>
      {% endfor %}
    </div>
  </div>

  <div class="pp-section">
    <h3 class="pp-section__title">Publications</h3>
    <div class="pp-scroll">
      {% for pub in publications | sort(true, true, "date") %}
        <a href="{{ pub.url | url }}" class="pp-card">
          <div class="pp-card__media">
            {% if pub.image %}
              <img src="{{ pub.image | url }}" alt="{{ pub.title }}" loading="lazy">
            {% endif %}
          </div>
          <p class="pp-card__title">{{ pub.title }}</p>
          <p class="pp-card__meta">{{ pub.authors }} &middot; {{ pub.date }}</p>
        </a>
      {% endfor %}
    </div>
  </div>

</div>

<style>
.projects-publications {
  display: flex;
  flex-direction: column;
  gap: 2rem;
  margin: 1.5rem 0;
}

.pp-section__title {
  font-size: 1.1rem;
  font-weight: 600;
  margin-bottom: 0.75rem;
}

.pp-scroll {
  display: flex;
  gap: 1rem;
  overflow-x: auto;
  padding-bottom: 0.5rem;
  scroll-snap-type: x mandatory;
}

.pp-card {
  flex: 0 0 220px;
  scroll-snap-align: start;
  text-decoration: none;
  color: inherit;
  border-radius: 8px;
  overflow: hidden;
  border: 1px solid #e2e2e2;
  background: #fff;
  transition: box-shadow 0.15s ease, transform 0.15s ease;
}

.pp-card:hover {
  box-shadow: 0 4px 14px rgba(0, 0, 0, 0.08);
  transform: translateY(-2px);
}

.pp-card__media {
  position: relative;
  aspect-ratio: 16 / 9;
  background: #f3f3f3;
}

.pp-card__media img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.pp-card__badge {
  position: absolute;
  top: 8px;
  left: 8px;
  background: rgba(0, 0, 0, 0.65);
  color: #fff;
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.03em;
  padding: 2px 8px;
  border-radius: 4px;
}

.pp-card__title {
  font-size: 0.88rem;
  font-weight: 600;
  padding: 0.6rem 0.7rem 0.2rem;
  margin: 0;
  line-height: 1.3;
}

.pp-card__desc {
  font-size: 0.78rem;
  color: #666;
  padding: 0 0.7rem 0.7rem;
  margin: 0;
  line-height: 1.35;
}

.pp-card__meta {
  font-size: 0.75rem;
  color: #888;
  padding: 0 0.7rem 0.7rem;
  margin: 0;
}

/* thin scrollbar, webkit browsers */
.pp-scroll::-webkit-scrollbar {
  height: 6px;
}
.pp-scroll::-webkit-scrollbar-thumb {
  background: #ccc;
  border-radius: 3px;
}
</style>
